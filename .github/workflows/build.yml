name: Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "a"

  b:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "b"

  c:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "c"

  d:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "d"
