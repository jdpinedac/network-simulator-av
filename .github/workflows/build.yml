name: Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "a"

  b:
    runs-on: ubuntu-latest
    steps:
      - run: echo "b"

  c:
    runs-on: ubuntu-latest
    steps:
      - run: echo "c"

  d:
    runs-on: ubuntu-latest
    steps:
      - run: echo "d"
